/**
 *
 * operador ternário : abreviação de if else
 * geralmente utilizado quando precisamos atribuir um valor para uma variavel,dependendo de uma condicao
 */

var idade = 18;
var podeBeber = idade >= 18 ? "pode beber" : "nao pode beber";
console.log(podeBeber);


// condição ? true : false
